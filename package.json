{
  "name": "crypto-screener",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.2.2",
  "scripts": {
    "dev": "NODE_OPTIONS=--max-old-space-size=6144 turbo dev",
    "build": "NODE_OPTIONS=--max-old-space-size=6144 turbo build",
    "start": "NODE_OPTIONS=--max-old-space-size=6144 turbo start",
    "lint": "turbo lint",
    "db:migrate": "turbo db:migrate",
    "db:generate": "turbo db:generate"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  }
}
